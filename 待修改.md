# 待修改事项

> 每条记录标注【前端】或【后端】（或两者），并记录涉及文件与建议方案。

---

## 1. 前端无法获取订单的下单时间（create_time 始终为空）【后端】

**状态：✅ 已完成（2026-08-28）**

**最终方案**
- 关联键验证结论：快照订单的 `order_id` 是引擎内部序号，与 CSV 无对应；**`order_local_id` ↔ csord CSV `orderid` 100% 命中**（SZ 7309 笔、SH 253 笔采样均 100%）。
- `TradeBook` 初始化后后台线程懒构建 `orderid → datetime` 映射（3 万条约 0.2s/4MB），快照三个接口返回前按 `order_local_id` 补 `create_time`。
- 映射构建后自动抽样 100 单自检命中率，<95% 打 warning 日志（兜底未来数据源变化）。
- 前端无需改动，`VolumeTable` tooltip 的"创建时间"自动生效（映射构建完成前短暂显示"暂无数据"）。

---

## 2. 日期列表超出数据源可用范围，选中后初始化报错【前后端】

**现象**
- 2026-08-28 实测：选择 2026-08-26 等近期日期初始化时报错 `cstra is accessible in the date range of ['2020-01-01', '2024-12-31']`。
- 原因：adata 数据源的 cstra 仅覆盖 2020-01-01 ~ 2024-12-31，而 `/tradebook/DateList` 从 SQL Server 交易日表取数、列到当天，两个范围不一致。

**建议方案**
1. 后端 `DateList` 返回中附带数据源可用区间（如 `available_range: {start, end}`，cstra/csord/cstick 取交集）。
2. 前端日期下拉把超出可用范围的日期置灰或加"无数据"标记，避免用户选了之后才发现报错。
3. 初始化失败时前端把后端的范围提示原文展示出来（当前已有，保持良好）。

**备注**
- 本地已缓存可直接用：000027.SZ/2025-08-01、000027.SZ/2022-01-07、600373.SH/2023-12-15。

**现象**
- 前端 `ordervis_client/src/views/orderbookQueue/volumeQueue.vue:1501`（另见 1930、2038 行）解析快照订单时读取 `order.create_time`，`VolumeTable.vue` 的悬浮提示中有"创建时间"一栏。
- 但后端快照接口（`snapshot_by_time` / `snapshot_by_id` / `snapshot_by_index`）返回的订单数据由 C++ 引擎（`ordervis_server/lib/pywangcai_orderbook.so`）生成，每个订单只包含 `order_id`、`order_local_id`、`remaining_volume`、`direction`、`price`，**没有 `create_time` 字段**。
- 结果：前端 tooltip 中"创建时间"永远显示"暂无数据"。

**需求**
- 前端需要能随时访问到任意一笔订单的下单时间，由后端提供该数据。

**数据来源**
- 下单时间已存在于逐笔委托数据 `ordervis_server/data/csord_{sym}_{date}.csv` 的 `datetime` 列（精确到毫秒），该文件在后端首次加载时即落盘缓存。
- 现有 `find_order` 接口（`routers/tradebook.py:391`）已在返回 `datetime`，可参考。

**建议方案（不改 C++ 模块，Python 层增强）**
1. `TradeBook` 初始化时（或首次需要时）读取 `data/csord_{sym}_{date}.csv`，构建 `orderid → datetime` 映射字典。
2. 在 `routers/tradebook.py` 的快照类接口返回前，遍历 `snapshot.levels.{bid1..3, ask1..3}.orders[]`，按 `order_id` 查表补充 `create_time` 字段。
3. 前端无需改动，tooltip 自动生效。

**待验证**
- [ ] C++ 快照中的 `order_id` / `order_local_id` 与 csord CSV 中的 `orderid` 能否一一对应（特别是上交所数据经 `sh_revert_module` 还原后）；若对不上，需改用 `seqno` 或 `bizindex` 做关联键。
- [ ] 大 CSV（全天逐笔委托）构建映射的内存与耗时是否可接受；必要时只在内存中保留 `orderid → 毫秒时间戳` 的轻量映射。
