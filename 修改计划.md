# OrderVis 修改计划 —— 量化高频订单可视化平台

> 合并初版修改点、`待修改.md`、`ordervis_client/docs/frontend-change-list.md` 及通读代码后的补充建议。
>
> **已确认的约束**：
> 1. 不动 C++ 动态库（`pywangcai_orderbook.so`），后端改动仅限 Python 层（内部调用现有引擎方法组合）。
> 2. 暗色主题为**可选**，浅色仍是默认。
> 3. ~~时间轴回放条~~（已取消，2026-08-28 调整：删去底部时间轴组件）。
>
> **最新布局决策（2026-08-28）**：
> - 页面两分：**左侧数据区** + **右侧图表区**（仅放图表）。
> - 左侧数据区内部也是左右结构：**左半买 1–3，右半卖 1–3**。
> - 右侧图表只保留两个：**C2 Q-t 窗口图**（横轴=时间窗口可选，纵轴=量、指标可选）和**锁定订单图表**（同理，纵轴为同档位身前量/身后量可选）。C1（Q-P 截面图）取消。

---

## 0. 项目现状与已有功能

### 已有功能
1. 按证券代码 + 日期，查看全天任意时刻卖 1–3 / 买 1–3 的价格与挂单量（队列到单笔订单粒度）。
2. 支持按时间移动和按订单变化移动（当前最小步进 50ms，待细化）。
3. 按下单时间 + 下单量查询订单（`find_order`）。
4. 按数量或订单 ID 锁定订单，并给出队列位置统计。

### 代码现状要点（通读后确认）
- 盘口数据全部走 HTTP，无轮询；WebSocket 仅用于初始化进度推送。
- `volumeQueue.vue`（3762 行）：`moveTimes` 步进时 **while 循环最多串行 200 个请求**（L1821-1861，现行代码，最大瓶颈）；差分统计旧版 6 请求实现已注释为死代码（L2763-2913），现行用 `pastTimeTradeInfo` 单请求。
- `VolumeTable.vue`：computed 中 3 次 `for (i<=10000)` 扫键，6 个网格每帧约 18 万次属性查找；全页面 183 处 console.log。
- 快照数据只有买卖各 3 档（`snapshot.levels.bid1..3 / ask1..3`），档内含每笔订单的 `order_id / remaining_volume / direction`，档位带 `price`。
- 页面硬编码浅色主题；echarts 基础设施（`useECharts`，含暗色支持）已存在但业务页未使用。
- 已有 issue：create_time 缺失、股票/基金未分型、同量多锁定订单统计混淆、订单查询不支持三位小数；**引擎撤单计数器恒为 0**（第1步发现，已在 Python 层修补，影响 `pastTimeTradeInfo` 与 C2 的撤单指标）。

---

## 1. 页面布局（新）

```
┌──────────────────────────────────────────────────────────────────┐
│ ① 顶栏(第1行): [类型▾][代码▾][日期▾][开始] │ 当前时刻           │
│    09:42:31.087 │ changeIndex #12,847/98,203 │ 价差 0.01 │ 状态 ●│
│ ① 顶栏(第2行·控制条): [小步 10ms▾][大步 1s▾] ◀◀ ◀ ▶ ▶▶ [跳转…]  │
│    [订单步进 ±1/50/100/500▾] [按量锁定…] [按ID锁定…] [清除]       │
├──────────────────────────────────┬───────────────────────────────┤
│ ② 左侧数据区（≈55%）              │ ③ 右侧图表区（≈45%，仅图表）   │
│ ┌──── 买区 ─────┬──── 卖区 ────┐ │ ┌─ C2 Q-t 窗口图 ────────────┐│
│ │ 买3  买2  买1  │ 卖1  卖2  卖3│ │ │ [窗口: 1s▾] [指标: 买一挂单▾]││
│ │ 队列格子网格   │ 队列格子网格  │ │ │  折线/面积图 + 竖游标        ││
│ │ (格子含订单量) │ (格子含订单量)│ │ └───────────────────────────┘│
│ └──────────────┴─────────────┘ │ ┌─ 锁定订单图表(条件显示) ──────┐│
│ 汇总指标行: 六档总量/单数·流量   │ │ [窗口: 1s▾] [身前量/身后量▾]   ││
│                                │ │  阶梯线 + 事件标记 + 摘要行     ││
│                                │ └───────────────────────────┘│
└──────────────────────────────────┴───────────────────────────────┘
```

要点：
1. **所有控制功能集中在顶栏**：顶栏分两行——第 1 行是标的选择与状态展示，第 2 行是控制条（颗粒度选择：小步 10/30/100ms、大步 500ms/1s/3s/10s/1min；时间步进、按时间跳转、订单步进、锁定入口）。原页面散落的四行按钮组全部收拢到这里，左侧数据区只保留纯数据展示，空间全部让给队列格子。
2. **买左卖右**贴合 A 股行情软件惯例；买区三档、卖区三档各自竖排（3 在左/右外侧，1 靠中间），中间天然形成"买卖分界"。
3. **C2 图表**：横轴=以当前时刻为右缘的滑动时间窗口（500ms/1s/3s/10s/1min 可选）；纵轴=量，指标可选：买一/卖一的**盘口挂单量**（一档累计等待量，2026-08-31 由"新增挂单量"改为状态量）、新增撤单量、新增成交量（共 6 个指标，单选或双选叠加）；图内竖游标与当前时刻对齐。
4. **锁定订单图表**：横轴同为滑动时间窗口；纵轴二选一——**身前量**（同价位队列中排在该订单前方的总剩余量）或**身后量**；叠加该订单的成交/撤单事件标记；顶部一行摘要（挂出时间/存活/成交占比/结局）。
5. 汇总指标行从两个 ant Table 压缩为一行紧凑数字块，把垂直空间还给队列格子。
6. 无锁定时锁定订单图表整卡收起；右侧图表区宽度可拖拽调整，窄屏（<1440px）收为底部抽屉。

---

## 2. 待办总表（✅=纯前端 ｜ 🔧=需后端 Python 层）

### A. 导航与颗粒度

| # | 待办 | 类型 | 说明 |
|---|---|---|---|
| A1 | 步进颗粒度选择器：小步 10/30/100ms，大步 500ms/1s/3s/10s/1min；订单步进 ±1/50/100/500 保留 | ✅ | 分段控件收进控制条 |
| A2 | **消灭串行请求风暴（关键路径）** | 🔧(优先) | 现行 `moveTimes`（volumeQueue.vue:1827-1861）最多串行 200 个请求。🔧 方案（第0步已验证）：Python 层新增 `next_change_after(time)`——`query_market_data(time)` 取当前 change_index i0（0.013ms），**i0+1 即下一个变化**，`query_by_change_index(i0+1)` 返回完整快照；每次请求仅 ≈25ms、2 次引擎调用，C++ 不动。**10ms 细颗粒度步进的前提** |
| A3 | 快照+差分合并为一个 `snapshot_bundle` 请求 | 🔧(低) | 每步 2 请求→1，收益有限，可延后；旧 6 请求注释代码（L2763-2913）随手删 |
| A4 | 快捷键：←/→ 小步、Shift+←/→ 大步、L 锁定、Esc 解除锁定 | ✅ | |

### B. 锁定订单增强

| # | 待办 | 类型 | 说明 |
|---|---|---|---|
| B1 | **锁定订单选择器**：按量锁定时弹列表（订单 ID+价位+方向+下单时间），用户多选后各自独立锁定；内置「按下单时间定位」入口（调 `find_order` 命中高亮置顶） | ✅ | 替代"盲锁第一个"；列表"下单时间"列依赖 B2（未就绪时显示"暂无数据"） |
| B2 | **create_time 补全** | 🔧 | 第0步已验证：用快照订单的 **`order_local_id`** 关联 CSV `orderid`（SZ 实测 100% 命中）。Python 层构建 `orderid→datetime` 轻量映射（3 万条约 0.7s/4MB，后台懒构建），快照返回前按 `order_local_id` 补 `create_time`。⚠️ 上交所 csord 是 `sh_revert_module` 用 cstra 反向重建的合成数据，订单号口径可能不同（未验证）。**实现要求：映射构建后自动自检**——抽样 100 个快照订单算 `order_local_id` 命中率，低于 95% 打 warning 日志（含标的与命中率），使 SZ/SH 及未来任何数据源的对齐问题第一时间在日志暴露，而非静默缺失 |
| B3 | 多锁定订单独立统计（队列位置/单前/本单/单后，同价位同方向各自算） | ✅ | 原 `frontend-change-list.md` #3；作为 B1 确认后的统计逻辑 |
| B4 | 订单生命周期接口 | 🔧(轻) | `GET /tradebook/order_lifecycle`：纯 pandas——csord 按 `orderid` 取出生，cstra 按 `bidorderid/askorderid` 关联成交(F)/撤单(4)事件，算出 remaining_after 序列+摘要（存活/成交占比/首笔延迟/结局）。供锁定图表的事件标记与摘要行使用 |

### C. 图表区（右栏，仅两个图）

统一用现有 `useECharts`（按需引入，自带暗色主题支持）。

| # | 待办 | 类型 | 说明 |
|---|---|---|---|
| C2 | **Q-t 窗口图**：横轴=滑动时间窗口（右缘=当前时刻，窗口 500ms/1s/3s/10s/1min 可选），纵轴=量、指标可选（买一/卖一 × 新增挂单/撤单/成交量，共 6 项，支持双选叠加） | 🔧(轻)+✅ | 🔧：`trade_flow_series(sym,date,time,window_ms)`，窗口内按固定采样点多次调 `query_market_data` 算差分序列一次返回（C++ 不动）。✅：echarts 折线/面积图，竖游标对应当前时刻；点击图上某点 → 主区跳转到该时刻 |
| C5 | **锁定订单身前/身后量图**：横轴=滑动时间窗口（与 C2 同控件），纵轴二选一：同档位身前量（该订单前方队列的总剩余量）/身后量；叠加成交/撤单事件标记（来自 B4）+ 摘要行 | ✅(+🔧可选) | 数据两路线：**路线一（默认）**前端浏览快照时本地累计——每帧从档内订单序列求和该订单前/后的 remaining_volume，零额外请求、不依赖 orderid 对齐；**路线二（可选增强）**🔧 `order_queue_series(order_id,time,window_ms)` 后端采样补全未浏览时段。无锁定时整卡收起 |

### D. 视觉与工程

| # | 待办 | 类型 | 说明 |
|---|---|---|---|
| D1 | 暗色主题为可选项，浅色默认；深色下买红卖绿重新调明度 | ✅ | |
| D2 | 等宽数字 tabular-nums + 毫秒时间等宽字体 | ✅ | 低成本高感知 |
| D3 | 订单格按剩余量热力梯度 + 进出过渡动画（≤200ms）；锁定订单改描边+角标 | ✅ | |
| D4 | 布局重构（按第 1 节）：买左卖右、控制条、汇总指标行压缩、右栏图表容器 | ✅ | |
| D5 | VolumeTable 渲染优化：废 v1..v10000 扫键（改数组/Map）、Set 替代线性匹配、删 computed 内 console.log | ✅ | |
| D6 | 拆分 volumeQueue.vue：`useTradebook`/`useSnapshotNavigation`/`useOrderLock`/`usePersistState` + 子组件；收敛 3 份重复 transform 逻辑；全页 console.log 改统一 logger | ✅ | "当前时刻"唯一状态由 useSnapshotNavigation 持有，所有导航入口（按钮/快捷键/图表点击）走同一 `seek()`，保证联动不脱节 |
| D7 | 修复持久化矛盾（onMounted 清空 localStorage vs watch 写入）与自动锁定 setTimeout 魔法链 | ✅ | |
| D8 | 清理 vben 示例残留（dashboard/simple/mock）、未用重依赖（Tinymce/Excel 等）、半废弃数据生成页、log.txt | ✅ | |
| D9 | 股票/基金分型 + 价格精度全局统一（股票 2 位/基金 3 位） | 🔧(`symList` 带类型)+✅ | |
| D10 | 订单查询支持基金三位小数价格，发送前不截断 | ✅ | 依赖 D9 |

---

## 3. 分步实施计划

### 第 0 步：动手前验证 ✅（2026-08-28 已完成，脚本 `ordervis_server/test/verify_step0.py`）

**结论（基于 000027.SZ / 2025-08-01，29860 行逐笔委托）**：

1. **orderid 对齐 ✅ 已解决，关联键是 `order_local_id`**
   - 快照订单的 `order_id` 是引擎内部序号（1..~59700 连续，约 2× 委托笔数），与 CSV `orderid`（交易所稀疏 ID）**无对应关系**（命中率 0.3%）。
   - 但快照订单的 **`order_local_id` 与 CSV `orderid` 100% 命中**（6 个时点 7309 笔全命中），且 99.9% 价格/方向/剩余量≤原始量一致（例外为市价单，CSV 中 price=0）。
   - → B2 补 `create_time`：构建 `orderid → datetime` 映射，按快照订单的 `order_local_id` 查表。B4 生命周期同样用此键。
   - ✅ SH 验证已完成（2026-08-28）：600373.SH / 2023-12-15 经后端流水线（下载→转换→sh_revert→落盘）生成数据后，对齐校验 **253/253 命中（100%）、价格/方向/量 100% 一致**——`order_local_id ↔ orderid` 关联键对 SH 还原数据同样成立。create_time/next_change/order_lifecycle/trade_flow_series（含撤单修补）在 SH 标的上全部 HTTP 实测通过。通用校验脚本：`test/check_alignment.py <sym> <date>`。自动自检机制（B2 实现要求）作为长期兜底。
2. **引擎耗时 ✅ 实测**：`query_by_time` 26.6ms/次、`query_by_change_index` 23.9ms/次、`query_market_data` **0.013ms/次**（含 timestamp 和 change_index）。
   - → A2 最优设计：`query_market_data(time)` 拿到当前 change_index i0（微秒级），**i0+1 即下一个变化**（已验证 change_index 时间戳单调递增），再 `query_by_change_index(i0+1)` 取完整快照——**每次请求仅 2 次引擎调用 ≈25ms**，无需二分（二分备选 ~300ms/15 次调用）。
3. **映射构建开销 ✅ 可接受**：29860 条耗时 736ms、峰值内存 4.1MB。大标的（~50 万行）预估 ~12s/~70MB → 建议在 TradeBook 初始化后**后台线程懒构建**，不阻塞主流程。

### 第 1 步：后端三个接口 ✅（2026-08-28 已完成并 HTTP 实测通过，验证脚本 `ordervis_server/test/verify_step1.py`）
- [x] `next_change_after(time)`（A2）——实测 ~20ms/次；下一/上一变化双向支持，尾盘边界返回"没有更多变化"
- [x] 快照返回补 `create_time`（B2）——`order_local_id` 映射后台懒构建（0.2s 就绪），741/741 订单补全；自动对齐自检已生效（日志：`命中率 100.0%`，<95% 会打 warning）
- [x] `trade_flow_series(time, window_ms)`（C2）——60 桶 1ms 级返回；**重要发现：引擎撤单计数器恒为 0（坏），撤单量已用 CSV 在 Python 层修补**（挂单价==撤单时刻一档价判定）
- [x] `order_lifecycle`（B4）——129ms 返回完整事件流+队列位置（含身前/身后量）+摘要
- [x] `symList` 返回证券类型字段（D9 后端部分）——`data: [{code, type: stock|fund|unknown}]` ⚠️ **前端需同步适配**（原为字符串数组，第 2 步 D9 处理）
- [x] 附带修复：`pastTimeTradeInfo` 的撤单指标同样恒 0，已用同一修补逻辑修复（现有汇总表撤单数据立即变正确）

### 第 2 步：前端数据正确性 ✅（2026-08-28 完成，生产构建通过）
- [x] B1 锁定订单选择器：新增 `components/LockOrderSelector.vue`——按量锁定改为弹列表（订单 ID+档位+方向+价格+剩余量+下单时间），用户勾选后批量锁定；内置「按下单时间定位」（调 `find_order`，命中行高亮+自动勾选，不在当前六档则明确提示）
- [x] B3 多锁定订单独立统计：`VolumeTable.vue` 的 `lockedOrderStats` 已按订单逐个独立计算（单前/本单/单后以档内全队列累计），B1 选择器补齐了"多锁"入口，该项闭环
- [x] D9 前端：`securityType.ts` 的 `explicitSecurityType` 支持后端 `{code, type}` 字段（unknown 落回前缀启发式），主盘口页与数据生成页经 `toSymbolOption` 自动生效
- [x] D10：`GlobalOrderSearch.vue` 价格输入精度/步长按证券类型切换（股票 2 位/0.01，基金 3 位/0.001），查询结果价格按精度格式化，文案"股票"→按类型动态显示
- [x] 附带：API 层新增 `getNextChange`/`getTradeFlowSeries`/`getOrderLifecycle` 封装（第 3/5 步使用）
- 验收：✅ `vite build` 生产构建通过；⏳ 浏览器端联调（悬停显示 create_time、按量锁定弹选择器、基金三位小数查询）待用户在页面实测

### 第 3 步：性能与结构 ✅（2026-08-28 完成，生产构建通过 + next_change 六场景实测）
- [x] 前端接入 A2 接口替换 moveTimes 循环：`moveTimes` 重写为「① 查目标时间快照 → ② 时间戳与当前相同则调 `getNextChange` 取相邻变化」，**每次步进 ≤2 个请求**（原最多 200 个串行）；无更多变化时恢复原时间并提示
- [x] **附带发现并修复后端引擎坑位**：`query_market_data`/`query_by_time` 的 `change_index` 会滞后 1 位、`query_by_change_index` 越界钳制到端点快照 → `get_adjacent_change` 改为「快照时间戳为基准 + ci±1 双候选 + 钳制防护」，SZ/SH 两市场的头部/尾部/中段六种场景实测全部正确
- [x] D5 VolumeTable 渲染优化：废 v1..v10000 扫键（改连续键 v1..vN 遇空缺即停）、`lockedIdSet` Set 替代线性 some、删 computed 热路径 console.log
- [x] D7 持久化与 setTimeout 链修复：明确 localStorage 语义「会话内跨组件共享、刷新清零」（修复 securityType 清理遗漏；删除恒为 false 的死 Pinia 分支与死的 initializePageData 恢复逻辑）；`handleGlobalSearchLockRequest` 三层 setTimeout（1500/2000/1000ms）改为顺序 await + 条件轮询等待首个快照（30s 超时）；`processVolumeData`/`clickMoveTime` 返回 Promise
- [x] 结构清理：删 150 行注释掉的旧 updateMarketData 死代码；3 份重复 `transformOrdersToTableData` 收敛为 1 个共享函数；volumeQueue.vue 120 处 `console.log` 改为 `debugLog`（仅 DEV 且 `localStorage.ov_debug='1'` 时输出）
- [ ] D6 完整拆分 volumeQueue.vue 为 composables —— **建议并入第 4 步**：布局重构（左数据区/右图表区）时模板必然重组，届时顺势拆分比现在纯逻辑抽取更自然、回归风险更低
- 验收：✅ 单次步进 ≤2 请求（实测）；✅ `vite build` 通过；✅ next_change 响应 ~25ms；⏳ 页面回归（步进/跳转/锁定/自动锁定流程）待用户实测

### 第 4 步：布局与视觉重构 ✅（2026-08-28 完成，生产构建通过）
- [x] D4 新布局：模板整体重写——① 顶栏两行（第1行：类型/代码/日期/开始 + 当前时刻/changeIndex/价差/已锁定/暗色开关；第2行控制条：时间小步+大步、订单步进、跳转、锁定入口）；② 左数据区 55%（买左卖右，每区三档竖排 买3→买1｜卖1→卖3，1 档靠中间分界）；③ 右图表区 45%（C2 + 锁定订单图占位卡，第 5 步接入）；原四行按钮组与两个 250px ant Table 全部移除
- [x] 汇总指标行：六档 量/单数 + 一档 挂/撤/成（/3s、/1min）紧凑数字条
- [x] A1 颗粒度选择器：小步 10/30/100ms、大步 500ms/1s/3s/10s/1min、订单步进 ±1/50/100/500，全部下拉可选
- [x] A4 快捷键：←/→ 小步、Shift+←/→ 大步、Ctrl(Alt)+←/→ 订单步进；输入框聚焦时不劫持
- [x] D1 暗色可选：顶栏 Switch，`.ov-dark` 作用域覆盖顶栏/图表卡/汇总条/队列格子，偏好存 `ov_theme`（跨刷新保留）
- [x] D2 等宽数字：`.num`/`volume-cell` 加 `font-variant-numeric: tabular-nums`
- [x] D3 热力梯度：VolumeTable 格子底色深度 ∝ 单量/档内最大单量（买红卖绿，alpha 0.06~0.48），0.25s 底色过渡动画；VolumeTable 新增 `cols` prop（普通 6 列/全屏 24 列）
- [ ] volumeQueue.vue 脚本层 composable 拆分仍挂起（模板已重构，脚本拆分留待第 5 步图表联动时一并抽取 useSnapshotNavigation）
- 验收：✅ `vite build` 通过；✅ 窄屏 <1440px 图表区自动收到底部；⏳ 深浅主题切换、快捷键手感、买左卖右布局待用户页面实测

### 第 5 步：图表区 ✅（2026-08-31 完成，生产构建通过）
- [x] C2 Q-t 窗口图（`components/TradeFlowChart.vue`）：窗口 500ms/1s/3s/10s/1min 可选；6 指标 Checkbox 最多双选叠加（买一/卖一 × 挂单/撤单/成交，买红卖绿明度区分）；横轴 value 轴用当日毫秒数（规避 echarts time 轴时区偏移），刻度精度随窗口自适应；竖游标 markLine 锚定当前时刻；数据走 `trade_flow_series`（60 桶，120ms 防抖 + 请求序号防过期响应）
- [x] C5 锁定订单身前/身后量图（`components/LockedOrderChart.vue`）：**路线一本地累计**——每帧快照从六档 orders 数组定位锁定订单（order_local_id）并求和身前/身后 remaining_volume，零额外请求；阶梯线 + 身前/身后量 Radio 切换 + 窗口选择；B4 `order_lifecycle` 每订单取一次并缓存，成交/撤单事件以 markPoint 标记（纵轴取值优先用事件自带队列位置，否则找本地最近点），顶部摘要行（挂出时间/存活/成交占比/结局，每订单一条彩色 chip）；换标的/日期自动清空累计与缓存；单订单 5000 点上限防内存膨胀；无锁定时整卡收起
- [x] 图↔格联动：双图点击某点 → emit seek → 父组件 `handleChartSeek` 统一走 `clickMoveTime`（与顶栏跳转同路径）；步进/跳转 → 双图随 currentTime 左滚（C2 重新取窗口序列，C5 本地累计追加）
- [x] D6 部分落地：抽取 `composables/useSnapshotNavigation.js`——selectTime/changeIndex/orderIndex/snapshotId 唯一状态源 + 全部时间工具函数（parseTimeToMs/formatMsToTimeStr/extractTimeFromTimestamp/checkTimeBoundary 等），volumeQueue.vue 删除 ~130 行重复定义；顺带修复 parseTimeToMs 对微秒级时间戳（.123456）的解析错误
- [x] 基础设施修复：echarts 按需注册补 MarkLine/MarkPoint 组件（否则游标与事件标记静默不渲染）；useECharts 的 theme 参数支持 MaybeRef（业务页暗色开关可驱动图表主题重建）
- ⚠️ 已知边界：暗色切换会 dispose 重建图表实例，click 绑定按实例去重重挂（已处理）；C5 仅覆盖浏览过的时段（路线二后端采样补全仍为可选增强，未做）
- 验收：✅ `vite build` 通过；⏳ 步进双图左滚、图点击 seek、锁定曲线与格子统计一致性待用户页面实测

### 第 6 步：收尾清理 ✅（2026-08-31 完成，生产构建通过）
- [x] D8 删除示例页/重依赖/半废弃页：
  - 页面与路由：删 `views/dashboard`（analysis/workbench）、`views/simple`、`views/sys/about` 及对应路由模块 `dashboard.ts/simple.ts/about.ts`；删半废弃 `ordersDataGenerate.vue` 及其路由与菜单注释；删登录页已注释的 QrCodeForm/MobileForm/RegisterForm
  - mock：删 `mock/demo/`，**保留 `mock/sys/`**（登录/getUserInfo/getPermCode 依赖，`VITE_USE_MOCK=true` 仍生效）
  - 死代码：删 volumeQueue.vue 的 minio 死导入与未调用的 `scrollToLockedOrderOld`（~140 行）；删 `services/minioService.js`；删孤儿组件 `components/Excel`、`components/Qrcode`、`components/Tinymce` 及 `public/resource/tinymce` 静态资源；删 `log.txt`；删 dashboard/demo 语言包文件
  - 依赖：移除 clickhouse、@clickhouse/client、minio-js、tz-minio-upload_beta、csvtojson、x2js、mockjs、process、vuedraggable、xlsx、qrcode 及 5 个对应 @types（保留 qs/sortablejs/crypto-js/@zxcvbn-ts 等框架仍在用的）
- [x] A3：评估后**决定不实施**——A2 之后每次步进已是 ≤2 请求且单请求 ~25ms，合并为 snapshot_bundle 仅省 1 次 RTT，收益不抵后端改动与回归风险；旧 6 请求注释代码经确认已在第 3 步清理完毕
- [x] 文档更新：`docs/frontend-change-list.md` 四条 issue 均已标注完成（①股票基金分型 ②悬停 create_time ③同量多锁定独立统计 ④基金三位小数查询），与第 1–3 步实现记录一致，无需再改

### 依赖关系图
```
第0步验证 ──→ 第1步后端接口 ──→ 第5步图表联调
     └──→ 第2步前端修复 ──→ 第3步性能结构 ──→ 第4步布局视觉 ──→ 第5步
（第2步可与第1步并行；第6步穿插）
```

---

## 4. 风险与备注

- **最大技术风险**：orderid 对齐（影响 B2 的 create_time 与 B4 事件关联）。若对不齐，create_time 改用 seqno/bizindex 关联；锁定图改用纯前端本地累计路线。
- **A2 是所有体验优化地基**：细颗粒度步进、图表窗口随步进滚动的流畅度都建立在"一次请求拿到下一个变化"之上。
- C2/C5 窗口右缘始终锚定当前时刻——手动步进、跳转、图表点击都会推动窗口左滚，行为统一由 `useSnapshotNavigation.seek()` 保证。
